# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
/build
next-env.d.ts

# production
/dist

# misc
.DS_Store
*.pem
Thumbs.db

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env
.env*.local
.env.local

# vercel
.vercel

# typescript
*.tsbuildinfo

# editor
.idea
.vscode

# git bundles
*.bundle.git
resurrection-tech-bundle.git

# Governance Skills — generated outputs (runtime artefacts, not source)
/attestation/
/audit-pack/
/governance-service/sectors_scaffold/

# python caches
__pycache__/
*.pyc
/threat-model/
/ship/

# assess-agent output
/assessment/

# Delivery-kit output (customer data — do not commit)
/deliverables/

# Delivery-kit local config (secrets)
.env.delivery

# Analyst Console local data (private client records)
/console/data/

# Locally downloaded browser (@puppeteer/browsers / playwright) — never commit
/chrome/

# Runtime Governance gateway local store (customer runtime data — never commit)
/.runtime-data/
/deliverables/runtime-reports/
.env*
.env.production

# Playwright
/test-results/
/playwright-report/
/playwright/.cache/

# CI-only staging for the sovereign engine image build (see .github/workflows/sovereign.yml)
governance-service/ci-sovereign/
